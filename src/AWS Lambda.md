# AWS Lambda

## Important Research Topics

- Serverless functions and their advantages
- Configuring handlers and supported runtimes
- Managing IAM roles for Lambda execution
- Lambda function invocation methods (manual, async, sync)
- Invoking Lambda through AWS services (S3, SQS, SNS, EventBridge)
- Lambda environment variables and secrets
- Lambda layers and dependencies
- Lambda timeout and memory configuration
- Error handling and retry policies
